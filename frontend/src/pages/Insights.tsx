import { Placeholder } from '../components/ui'

export default function Insights() {
  return (
    <Placeholder
      heading="Insights"
      arriving="Arrives in Sprints 4-5, built on the evidence and lineage foundations from Sprint 1."
      bullets={[
        'Multiple hypotheses tested with evidence for and against, not a single asserted cause',
        'Retrieval over the company documents stored here, filtered by entitlement before search',
        'Confidence scoring with genuine abstention when evidence is insufficient or contradictory',
        'Persona-specific narratives from one insight contract — same facts, different depth',
        'Recommendations shaped as driver → lever → action → owner → expected impact',
      ]}
    />
  )
}
